apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: bankovni-kurzy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: bankovni-kurzy/frontend:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: bankovni-kurzy
spec:
  selector:
    app: frontend
  ports:
    - port: 80
